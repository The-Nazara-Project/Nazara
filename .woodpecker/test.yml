matrix:
  RUST: [stable]

steps:
  run_tests:
    when:
      - event: push
        branch: main
      - event: pull_request
        branch: main
    image: rust
    environment:
      CARGO_TERM_COLOR: always
    commands:
      - rustup default $RUST
      - cargo test -j 4
